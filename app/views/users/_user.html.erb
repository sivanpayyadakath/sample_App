
<li>
  <%= gravatar_for user%>
  <%= link_to user.name, user_path(user) %>
  <% if current_user.admin? && !current_user?(user) %>
     <%= link_to "", user, method: :delete, class:"glyphicon glyphicon-trash",
                  data: { confirm: "Are you sure?" } %>
    <% end %>
</li>